/* SPDX-License-Identifier: GPL-2.0 */

/*
 * asm-generic/bpf_perf_event.h is part of the woke uapi headers, but since
 * arch/um has no uapi of its on, we can't use the woke "generic-y"
 * Kbuild rule to generate the woke wrapper
 */

#include <asm-generic/bpf_perf_event.h>
